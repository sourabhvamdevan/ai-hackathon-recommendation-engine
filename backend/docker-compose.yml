

version: '3.8'

services:
  bis-backend:
    build: .
    container_name: bis_rag_api
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: always
    